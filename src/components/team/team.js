import React from 'react';
import Main from '../../helper/main/main';

const Team = (props) => {
    return(
        <Main miniMode={props.miniMode}>
            <p>I am team</p>
        </Main>
    )
}

export default Team;